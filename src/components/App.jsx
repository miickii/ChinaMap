import React, { useState } from 'react';
import { Routes, Route } from 'react-router-dom';
import GameMap from './GameMap';
import GameSettingsModal from './GameSettingsModal';
import InfoModal from './InfoModal';
import FriendModal from './FriendModal';
import { motion, AnimatePresence } from 'framer-motion';

const App = () => {
  const [gameSettings, setGameSettings] = useState({
    lives: 3,
    timeLimit: 60,
    displayMode: 'English',
    mode: 'select'
  });

  return (
    <div className="min-h-screen bg-gray-50">
      <header className="bg-white shadow-sm">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-4">
          <div className="flex items-center justify-between">
            <h1 className="text-2xl font-bold text-gray-900">China Map Interactive</h1>
            <div className="flex items-center space-x-4">
              <button className="button button-secondary">
                <span className="sr-only">Info</span>
                <svg className="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
                </svg>
              </button>
              <button className="button button-primary">
                <span className="sr-only">Settings</span>
                <svg className="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.065 2.572c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.572 1.065c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.065-2.572c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z" />
                  <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M15 12a3 3 0 11-6 0 3 3 0 016 0z" />
                </svg>
              </button>
            </div>
          </div>
        </div>
      </header>

      <main className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        <AnimatePresence mode="wait">
          <motion.div
            initial={{ opacity: 0, y: 20 }}
            animate={{ opacity: 1, y: 0 }}
            exit={{ opacity: 0, y: -20 }}
            transition={{ duration: 0.3 }}
            className="map-container"
          >
            <GameMap gameSettings={gameSettings} />
          </motion.div>
        </AnimatePresence>
      </main>

      <Routes>
        <Route path="/" element={<GameMap gameSettings={gameSettings} />} />
        <Route path="/settings" element={<GameSettingsModal onSettingsChange={setGameSettings} />} />
        <Route path="/info" element={<InfoModal />} />
        <Route path="/friends" element={<FriendModal />} />
      </Routes>
    </div>
  );
};

export default App;
